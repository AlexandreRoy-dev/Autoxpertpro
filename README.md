# AutoXpert

Live site: https://autoxpertpro.com/

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env.local` and add `AI_GATEWAY_API_KEY` to stream the mechanic assistant. Without the key, the catalogue matcher still answers.
